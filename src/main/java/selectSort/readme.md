###    选择排序

####    算法简介
选择排序是不稳定的内排序，算法思想很简单:首先在待排序中选出最小元素，放置序列
起始位置，然后再从待排序元素选出最小元素，直至遍历完每个元素。
####    算法实现步骤
1.  起始状态:无序区[1,2...,n],而有序区为空。
2.  从无序区选择第一个元素的下标索引作为最小值索引，依次与剩余的无序区元素比较，
若找到元素比最小值索引值小，则保存当前元素索引，直到遍历完剩余的无序区元素，交换元素。
3.  重复步骤1-2。
####    算法动态图
![selectSort](select.gif "插入排序")